import 'package:flutter/material.dart';

class CarWashCheckOutTab extends StatelessWidget {
  const CarWashCheckOutTab({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
